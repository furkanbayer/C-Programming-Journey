#include <stdio.h>
#include <math.h>

int main() 
{
    float x = 3;
    
    // x = sqrt(x);   calculates the square root
    // x = pow(x, 4);  raises to a power (x to the power of 4)
    // x = round(x);   rounds to the nearest integer
    // x = ceil(x);    rounds up (ceiling)
    // x = floor(x);   rounds down (floor)
    // x = fabs(x);    calculates the absolute value (Note: fabs for floats, abs for ints)
    // x = log(x);    calculates the natural logarithm
    // x = sin(x);     calculates the sine
    // x = cos(x);    calculates the cosine
    // x = tan(x);     calculates the tangent

    printf("%f", x);
    
    return 0;
}